# dotfiles

May or may not work on your machine, this is mostly just a notebook for myself.

```bash
sudo apt install git
sh -c "$(curl --location https://taskfile.dev/install.sh)" -- -d -b ~/go/bin
git clone git@github.com:samharju/.dotfiles.git
cd .dotfiles
task
```
